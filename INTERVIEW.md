# PaperMind 面试问答手册（研究生复试向）

结合本项目，把 RAG 的每个环节讲清楚。回答思路：**先说是什么 -> 再说项目里怎么做的 -> 最后说取舍/改进**。

---

## 1. RAG 是什么？为什么比微调更适合论文问答？

**答**：RAG（Retrieval-Augmented Generation）= 检索 + 生成：用户问题先去知识库里检索相关片段，再把片段作为上下文交给 LLM 生成回答。

相比微调的优势（结合本项目）：
1. **知识可即时更新**——新论文扔进 `data/docs` 重新摄入即可，不用重训模型；
2. **回答可溯源**——我的回答强制带 [1][2] 引用标注，对应具体文档的具体 chunk，可核验，能抑制幻觉；
3. **成本极低**——只需一个通用 LLM API + 向量检索，无需 GPU 训练。

## 2. 你的 RAG 全流程？（对着架构图讲）

**摄入链路**：`Loader(pypdf 抽 PDF 文本) -> Chunker(递归字符分割, 800/120) -> Embedder(三级降级) -> VectorStore(L2 归一化 + np.save 持久化)`

**问答链路**：`问题嵌入 -> VectorStore 余弦 top-k -> 阈值过滤 -> Generator 拼 Prompt -> DeepSeek 生成带引用回答`

## 3. 为什么分块？块大小怎么选？

**答**：嵌入模型有最大输入长度（MiniLM 是 256 token），且整篇论文嵌入会"语义稀释"——一个向量表示 10 页内容，什么都像又什么都不像。块小则上下文碎片化，LLM 拿不到完整论证。

我选 **800 字符 + 120 重叠**：约等于论文一个段落/小节，语义完整；重叠保证跨块边界的句子两边都能被检索到。递归分割的顺序是 段落→句子→词，优先在自然语义边界切。

## 4. 为什么选多语言 MiniLM？你的嵌入怎么处理"中文问英文论文"？

**答**：这是跨语言检索问题。`paraphrase-multilingual-MiniLM-L12-v2` 是多语言模型，中英文语义相近的句子会被映射到向量空间中相近的位置，所以中文问题"如何做异常检测"能直接命中英文论文中 "anomaly detection" 的段落。

我还做了**三级降级**：API 嵌入 → fastembed 本地 ONNX 推理 → 词/字符 n-gram 哈希投影兜底，保证无网/无 Key 环境系统仍可运行（只是降级为词面匹配）。

## 5. 向量检索为什么用 NumPy 而不用 FAISS？

**答**：规模决定方案。我的库只有几千个块，384 维，**暴力矩阵乘法一次就是全部打分**，延迟 <10ms，精确检索（exact search）。FAISS 的价值在 ANN（近似最近邻，IVF/HNSW 索引）换速度，只有百万级向量才有意义。用 NumPy 还让整个流程透明，便于调试和讲原理：**余弦相似度 = L2 归一化后的点积**。

## 6. 怎么减少 LLM 幻觉？（高频题）

**答**：我在三层做了约束：
1. **检索层**：相似度阈值 0.30 过滤——低相关片段本身就是噪声，会给 LLM"自由发挥"的素材；
2. **提示词层**：System Prompt 明确"仅基于给定片段回答，不足则说未涉及"；
3. **溯源层**：片段带编号，要求回答标注 [n]，用户可回查原文核验。

本质上 RAG 不是消灭幻觉，而是**把开放式生成约束成有据可查的阅读理解**。

## 7. 检索效果不好怎么优化？（调优思路）

**答**：按定位顺序排查：
- **查准差** → 提高阈值、换更强的嵌入模型（bge-m3）、加 reranker（如 bge-reranker 对 top-20 重排取 top-5）；
- **查全差** → 稠密+BM25 混合检索（Hybrid），RRF 融合；专业术语（PatchCore/MVTec）词面匹配往往更稳；
- **块截断关键信息** → 调整 chunk_size/overlap，或按章节结构切分（利用 PDF 大纲）；
- **评估** → 自建评测集（问题+标准出处 chunk），算 Recall@k / MRR 再对比调参，不能凭感觉。

## 8. 项目里 CV 异常检测方向包含了哪些论文/技术路线？（领域题）

**答**：知识库覆盖了工业异常检测主流路线：
- **特征记忆库/最近邻**：SPADE（SPADE, CVPR'21）、PatchCore（_coreset 采样 + memory bank，CVPR'22）、PaDiM（块分布建模，高斯参数化）
- **自监督/合成异常**：CutPaste（自监督代理任务）、DRAEM（重构+判别的合成异常训练）
- **蒸馏**：Reverse Distillation（教师-学生特征差异）
- **归一化流**：FastFlow（特征分布建模为 normalizing flow）
- **判别式端到端**：SimpleNet（合成伪特征 + 轻量判别器）
评测基准是 MVTec AD。

可以延伸：这条线核心假设是"只用正常样本训练，测试时偏离正常分布即为异常"，和 RAG 里"找最相关的知识"在数学形式上都是**最近邻/分布距离**问题。

## 9. 如果并发/数据量变大，架构怎么演进？

**答**：
- 向量库换 FAISS/Milvus（ANN 索引），元数据过滤（按年份/venue）；
- 摄入改异步队列（Celery），增量更新 + 版本管理；
- 嵌入推理批量+缓存（同 chunk 不重复算）；
- 生成层加流式输出（SSE）、限流、多模型 fallback。

## 10. 你遇到的最难的技术问题？

**答**（真实经历）：中文问题检索英文论文完全不准——一开始用哈希嵌入兜底，中英词表完全不相交，相似度全是 0。定位到是**跨语言语义对齐**问题后，引入多语言 MiniLM（ONNX 推理），中英同义句余弦从 ~0 提升到 0.7+，检索立刻命中正确段落。教训：**嵌入模型的"语言覆盖"必须和 Query/Doc 语种匹配，这是 RAG 里最容易被忽略的一环**。

## 11. 除了 RAG 本身，项目还有哪些工程化实践？（区分度题）

**答**：我把项目做成了**每天自动运行的真实服务**，不是一次性 demo：

1. **CI/CD 自动化**：GitHub Actions 定时任务（cron）每天 09:00 运行 `scripts/daily_digest.py`：调用 arXiv API 抓最新论文 → LLM 结构化输出（JSON 解析+降级兜底）→ 生成 Markdown 日报 → bot 身份自动 commit/push 回仓库；
2. **幂等与去重**：用 `seen.json` 记录已推送的论文 ID，任务重跑/周末无新论文都不会重复推送；当天文件重复生成时自动合并；
3. **密钥安全**：API Key 走 GitHub Secrets + .env 双通道，永不进代码库（.gitignore 校验过）；
4. **降级设计贯穿全链路**：LLM 失败→规则兜底；本地模型失败→哈希嵌入；无新论文→跳过提交，任何单点故障都不会让定时任务挂掉。

这套东西面试官一听就知道你真的"运维"过一个系统，而不是写完就扔的课程作业。

## 12. 这个项目的实际应用价值是什么？

**答**：两个真实使用场景——
1. **每日论文日报**：研究者每天 1 分钟掌握领域最新论文（中文解读+推荐评级），比自己刷 arXiv 快得多；仓库本身就是可订阅的"期刊"；
2. **论文精读问答**：读论文时把 PDF 扔进知识库，用中文追问细节（"实验对比了哪些 baseline"），回答带原文引用可溯源，省去反复翻原文；
3. 长期价值：日报持续沉淀领域文献库，知识库自动"生长"。

## 13. 你怎么保证代码质量？测试怎么设计的？（工程素养题）

**答**：31 个 pytest 用例，分三层：

1. **单元测试**：分块边界（大小/重叠/序号）、向量库（余弦值域/持久化roundtrip/按源删除）、哈希嵌入（确定性/归一化/词面重叠>无关句）、检索（阈值过滤/top-k截断，用 StubEmbedder 隔离嵌入实现）；
2. **集成测试**：临时目录写入真实文档，走完 摄入→检索→生成 全链路，断言"anomaly 问题命中 patchcore.txt 而不是 cooking.txt"（端到端行为而非实现细节）；再验证索引持久化重载、重复摄入不膨胀（增量替换）；
3. **HTTP 契约测试**：Flask test_client 验证 400（空问题）/404/上传-问答回环。

**关键设计：测试全离线**——把嵌入 API 和本地模型标记为失败，强制走哈希兜底。好处：CI 不用下载 100MB 模型、不依赖网络和 API Key，0.4 秒跑完；而且**降级路径本身也被测试覆盖**（很多项目的兜底代码从来没被执行过，一用就炸）。CI 在 Python 3.10-3.12 矩阵上跑 ruff + pytest，push/PR 即触发。

## 14. 服务怎么部署的？

**答**：三层——本地开发 `python app.py`（Flask 内置）；CLI `papermind serve`；生产 Docker（python:3.11-slim + gunicorn 双 worker，data 目录挂卷持久化）。应用工厂 `create_app()` 让 WSGI 入口和测试共享同一套构建逻辑，Pipeline 懒加载避免启动即吞内存；`/api/health` 存活探针供负载均衡/容器健康检查用。
