# 🎓 复试面试：RAG 高频问题与回答思路

> 用法：先理解每道题的"回答骨架"，再对照本项目代码亲手跑一遍。
> 面试时最有力的句式是：**"这个我在我的项目里是这样实现的……"**——然后指出具体模块。

---

## 一、基础原理（必考）

### 1. 什么是 RAG？为什么需要它？
**骨架**：RAG（Retrieval-Augmented Generation，检索增强生成）= 检索系统 + 生成模型。先从知识库检索相关证据，再把证据注入提示词让 LLM 生成答案。
**为什么需要**（三点，逐条说）：
- **知识时效性**：LLM 训练有截止日期，RAG 可接入最新文档
- **幻觉控制**：让模型"开卷考试"而非"闭卷默写"，答案有据可查
- **私有知识**：企业/个人文档不可能都拿去微调，RAG 成本低、可随时更新

### 2. 请完整描述 RAG 的工作流程
**骨架**：分两阶段讲，边说边对应本项目模块：
- **离线索引**：文档解析（loader.py）→ 分块（chunker.py）→ 向量化（embeddings.py）→ 存入向量库（vectorstore.py）
- **在线问答**：问题向量化 → 相似度检索 Top-K（retriever.py）→ 组装 Prompt（generator.py）→ LLM 生成带引用答案

### 3. 什么是 Embedding？为什么能用来检索？
**骨架**：嵌入模型把文本映射为高维稠密向量，**语义相近的文本在向量空间中距离更近**。检索 = 把问题也变成向量，找空间里最近的文档块（最近邻搜索）。
**加分点**：提一句嵌入模型本质是双塔编码器（如 BERT 类），用对比学习训练，正样本对拉近、负样本对推远。

### 4. 向量检索和关键词检索（BM25）的区别？
| | BM25 | 向量检索 |
|---|---|---|
| 原理 | 词频-逆文档频率，字面匹配 | 语义空间距离 |
| 优点 | 精准匹配专有名词、快、可解释 | 理解同义改写、跨语言 |
| 缺点 | "汽车"搜不到"轿车" | 可能漏掉精确术语 |

**加分点**：生产系统常用**混合检索**（Hybrid Search）——两路召回后用 RRF（Reciprocal Rank Fusion）融合排序。

### 5. 余弦相似度怎么计算？为什么用它而不是欧氏距离？
**公式**（面试可能要手写）：`cos(q,d) = q·d / (|q|·|d|)`
**为什么**：余弦只关心方向不关心模长，对文本长度不敏感；向量归一化后余弦 = 点积，一次矩阵乘法算完（本项目 vectorstore.py 的 search 方法正是这样实现的）。

---

## 二、分块策略（高频追问）

### 6. 为什么要分块？chunk_size 怎么定？
**为什么**：① 嵌入模型有输入长度限制，长文本向量会"语义稀释"；② 检索需要细粒度定位；③ LLM 上下文有限。
**怎么定**：经验值 300~800 字符。太大→检索不精准、塞爆上下文；太小→语义不完整。本项目取 500，可通过 config.py 调参对比效果。

### 7. overlap（重叠）的作用？
防止关键句子正好落在块边界被切断——有重叠后，边界句子至少完整存在于某一个块中。本项目 overlap=80。

### 8. 你知道哪些分块方法？
- **固定长度**：简单但会切断语义
- **递归字符分块**（本项目）：按 段落→句子→词 逐级切，边界落在自然语义处
- **语义分块**：用嵌入相似度检测语义断点
- **结构感知分块**：按 PDF 章节/标题树切，适合论文
**加分点**：论文有清晰的章节结构，结构感知分块是更优解，可作为"未来改进方向"主动提出。

---

## 三、幻觉与质量（必考）

### 9. RAG 如何控制幻觉？（本项目四条防线，逐条说）
1. **Prompt 约束**：系统提示明确"只依据资料回答，不足就说不知道"（generator.py SYSTEM_PROMPT）
2. **引用标注**：要求标注 [1][2] 来源，答案可溯源核查
3. **检索阈值**：相似度低于阈值的块直接丢弃，宁缺毋滥（retriever.py sim_threshold）
4. **低温度采样**：temperature=0.3，减少发散

### 10. 怎么评估 RAG 系统的效果？
**骨架**：分检索和生成两端评估：
- **检索端**：Recall@K（正确答案在不在 Top-K 里）、MRR
- **生成端**：忠实度（Faithfulness，答案是否忠于证据）、答案相关性
- **工具**：RAGAS 框架可自动评估上述指标
**加分点**：强调要先建**评测集**（问题+标准答案+对应文档），再谈优化——"没有评测的优化都是盲调"。

---

## 四、检索优化（进阶追问）

### 11. 检索效果不好怎么优化？
按优先级说：
1. **查询改写**：口语化问题改写成检索友好形式；多查询扩展
2. **HyDE**：先让 LLM 生成"假想答案"，用假想答案去检索（假想答案与真实文档的表述更接近）
3. **多路召回**：向量 + BM25 混合
4. **重排序（Rerank）**：初筛 Top-20 后，用交叉编码器（Cross-Encoder）精排到 Top-4——交叉编码器把问题和文档拼在一起过 BERT，判断更准但慢，所以只做精排

### 12. 交叉编码器和双塔编码器的区别？
- **双塔**（嵌入模型）：问题和文档各自编码，可预计算文档向量，检索快，但交互少
- **交叉编码器**（重排模型）：问题+文档拼接后联合编码，交互充分、更准，但无法预计算，只能对少量候选精排
- 所以工业界是"双塔召回 + 交叉编码器精排"的两段式

---

## 五、工程与前沿（展示视野）

### 13. 向量数据库怎么选型？
- 原型/小数据：FAISS、Chroma、纯 numpy（本项目，逻辑透明）
- 生产级：Milvus / Qdrant / pgvector，支持亿级向量、元数据过滤、分布式
- 索引算法：暴力（精确）/ IVF（聚类近似）/ **HNSW**（分层图索引，最常用，查询快、召回高）

### 14. RAG vs 微调，怎么选？
| | RAG | 微调 |
|---|---|---|
| 知识更新 | 换文档即可 | 重新训练 |
| 成本 | 低 | 高（算力+数据标注） |
| 适合 | 事实性问答、私有知识 | 风格/格式/领域语言能力 |
**加分点**：二者不互斥，实际系统常"微调让模型更懂领域语言 + RAG 注入最新事实"。

### 15. 你了解 Agentic RAG / GraphRAG 吗？（前沿加分题）
- **Agentic RAG**：LLM 作为智能体自主决定"要不要检索、检索什么、要不要再检索一轮"，多跳推理
- **GraphRAG**：先用 LLM 从文档抽取实体关系构建知识图谱，检索时结合图结构，擅长回答"全局性/关系型"问题
- **多模态 RAG**：检索对象扩展到图表、图片（论文里大量信息在图表中，这是论文问答的真实痛点）
> 这三个方向任选一个深入读 2~3 篇论文，面试时主动提，展示你跟进前沿。

---

## 六、针对本项目的追问（提前准备）

### 16. 你的项目为什么用 numpy 手写而不用 FAISS？
"为了吃透原理。暴力余弦检索在万级向量下足够快，而且每一行代码我都能讲清；FAISS 的 HNSW/IVF 我了解其原理（可展开），生产环境会选用。"——**先讲透再承认规模局限**，是标准答法。

### 17. 没有 API key 时你的系统怎么工作？
降级设计：嵌入用本地字符 n-gram 哈希向量（只有字面信号，我会明确说明它与真实语义嵌入的区别），生成用抽取式回答（直接返回最相关原文）。这体现了**系统的鲁棒性设计**。

### 18. 如果让你继续改进，下一步做什么？
准备 2~3 条具体的：① 结构感知分块（按论文章节）；② 混合检索 + Rerank；③ 图表解析（多模态）；④ 引入 RAGAS 评测集做量化调优。

---

## 📌 复试前 Checklist

- [ ] 手写一遍余弦相似度公式和 RAG 流程图
- [ ] 本项目每个模块都能说出"它解决什么问题、关键参数是什么"
- [ ] 亲手调一次 chunk_size / top_k，观察效果变化（有真实体感才答得生动）
- [ ] 准备一个 demo：上传目标导师方向的 1~2 篇论文，现场提问
- [ ] 选一个前沿方向（Agentic RAG / GraphRAG / 多模态）读 2 篇论文
